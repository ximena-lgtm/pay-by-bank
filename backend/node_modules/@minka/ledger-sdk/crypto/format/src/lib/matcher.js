"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isMatch = isMatch;
exports.isActionMatch = isActionMatch;
const mingo_1 = require("mingo");
const types_1 = require("../../../types/src");
const validation_1 = require("./validation");
/**
 * Check if data matches the given filter. It supports all the operators supported by MongoDB.
 * @param data Data to be checked against
 * @param filter Filter to be applied (can be a LedgerFilter or a Query object)
 * @param queryOptions
 * @returns true if data matches the filter
 */
function isMatch(data, filter, queryOptions) {
    // If no filter is provided, match everything
    if (!filter ||
        (typeof filter === 'object' && Object.keys(filter).length === 0)) {
        return true;
    }
    // If filter is already a Query object, use it directly
    if (filter instanceof mingo_1.Query ||
        (typeof filter === 'object' &&
            'test' in filter &&
            typeof filter.test === 'function')) {
        return filter.test(data);
    }
    const query = new mingo_1.Query(filter, queryOptions);
    return query.test(data);
}
/**
 * Determines whether access rule actions are included or excluded
 * by evaluating a match expression against a given query expression.
 * @param matchExpression - The expression used to evaluate matches.
 * @param actionExpression - The access rule action expression to compare.
 * @returns - Returns `true` if the match expression includes or excludes
 *                      the actions as per the query; otherwise, `false`.
 */
function isActionMatch(matchExpression, actionExpression) {
    let actionsToMatch;
    if ((0, validation_1.isInAggregation)(actionExpression)) {
        actionsToMatch = actionExpression.$in;
    }
    else if ((0, validation_1.isNinAggregation)(actionExpression)) {
        if (actionExpression.$nin.includes(types_1.AccessAction.Any)) {
            actionsToMatch = [];
        }
        else {
            const excludedActions = actionExpression.$nin;
            actionsToMatch = types_1.AccessActions.filter((action) => {
                return !excludedActions.includes(action);
            });
        }
    }
    else {
        actionsToMatch = [actionExpression];
    }
    return isMatch({ action: actionsToMatch }, { action: matchExpression });
}
//# sourceMappingURL=matcher.js.map