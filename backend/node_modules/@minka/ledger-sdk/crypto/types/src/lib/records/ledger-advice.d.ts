import { LedgerAddress } from '../common/ledger-address';
import { LedgerCustom } from '../common/ledger-custom';
import { LedgerHandle } from '../common/ledger-handle';
export type AdviceAliasResolveResultValue = {
    handle: LedgerAddress;
    schema?: LedgerHandle;
    custom?: LedgerCustom;
};
/**
 * Alias resolve advice result
 */
export type AdviceAliasResolveResult = {
    values: Array<AdviceAliasResolveResultValue>;
};
/**
 * Alias resolve advice
 */
export type AdviceAliasResolve = {
    handle: LedgerAddress;
    symbol?: LedgerHandle;
};
