import { EventSignal } from '../../records/ledger-event';
import { EventDeliveryStatus } from '../../records/ledger-event-delivery';
import { LedgerHandle } from '../ledger-handle';
import { DefaultBaseSearchableFields, SearchableFields } from './base';
type BaseEventDeliverySearchableFields = {
    /**
     * Domain handle(s), or boolean in case of usage of `$isNull` operator.
     */
    'data.record': LedgerHandle;
    'data.linked': LedgerHandle;
    'data.bridge': LedgerHandle;
    'data.effect': LedgerHandle;
    'meta.output.data.signal': EventSignal;
    'meta.status': EventDeliveryStatus;
};
type EventDeliverySearchableFields = Partial<SearchableFields<BaseEventDeliverySearchableFields & DefaultBaseSearchableFields>>;
export { EventDeliverySearchableFields };
