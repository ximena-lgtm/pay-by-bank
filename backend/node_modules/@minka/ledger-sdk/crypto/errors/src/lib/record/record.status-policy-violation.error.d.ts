import { LedgerCustom, LedgerError, LedgerErrorReason } from '../common/index';
/**
 * Error thrown when a user attempts to set a status not supported by policies.
 */
export declare class RecordStatusPolicyViolation extends LedgerError {
    /**
     *
     * @param detail error detail - message
     * @param custom custom data record to attach
     *               additional info about the error
     */
    constructor(detail: string, custom?: LedgerCustom);
    static REASON: LedgerErrorReason;
}
