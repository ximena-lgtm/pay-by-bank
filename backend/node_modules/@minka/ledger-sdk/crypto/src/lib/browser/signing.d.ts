import { LedgerHash, LedgerKeyPair, LedgerProof, LedgerSignatureResult } from "../../../types/src";
export declare function signHash(hash: LedgerHash, keyPair: LedgerKeyPair): Promise<LedgerSignatureResult>;
export declare function verifySignature(hash: LedgerHash, signature: LedgerProof): Promise<boolean>;
