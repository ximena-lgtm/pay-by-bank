/**
 * @module RecordBuilder / RetryEventBuilder
 * @category Record Builders
 */
import { LedgerRecord } from "../../../types/src";
import { BaseCompositeBuilder } from '../common/services/base-composite-builder';
import { RequestOptions } from '../common/types/request-options';
export type EventRetryData = {
    handle: string;
};
type SendRetryEventFunction = (record: LedgerRecord<EventRetryData>, reqOptions?: RequestOptions) => Promise<void>;
/**
 * Builder for retrying a specific event delivery by handle.
 *
 * @example
 * await sdk.bridge
 *   .with(bridgeHandle)
 *   .events
 *   .retry({ handle: eventHandle })
 *   .hash()
 *   .sign([{ keyPair }])
 *   .send()
 */
export declare class RetryEventBuilder extends BaseCompositeBuilder<EventRetryData, void> {
    private sendRetry;
    constructor(sendRetry: SendRetryEventFunction, payload: EventRetryData);
    send(requestOptions?: RequestOptions): Promise<void>;
}
export {};
