import { EntitySortOptions, EventDeliverySearchableFields, EventDeliverySortableFields } from "../../../types/src";
import { BaseListParams } from '../common/types/list-params';
/**
 * Query parameters for listing event deliveries.
 * Filter prefixes follow the ledger convention: `meta.` for status, `data.` for record fields.
 */
export type EventDeliveryListParams = BaseListParams & EventDeliverySearchableFields & EntitySortOptions<EventDeliverySortableFields>;
