/**
 * @module Clients / Event Delivery
 * @category Entities
 */
import { AxiosResponse } from 'axios';
import { EventDeliveryMeta, LedgerEventDelivery, LedgerPagedList, LedgerRecord } from "../../../types/src";
import { BaseRecordResponse } from '../common/types/base-record-response';
import { BaseListResponse } from '../common/types/list-response';
/**
 * List response for event deliveries.
 * For access to `status`, `replay`, and `proofs` use `response.data.data` and read from each record's `meta`.
 */
export declare class EventDeliveryListResponse extends BaseListResponse<LedgerEventDelivery, EventDeliveryMeta> {
    /**
     * List of event delivery records (data only).
     * To access delivery status, replay count, or proofs, use `response.data.data[i].meta`.
     */
    events: LedgerEventDelivery[];
    constructor(response: AxiosResponse<LedgerPagedList<LedgerEventDelivery, EventDeliveryMeta>>);
}
/**
 * Single event delivery record response, used by the
 * `GET /:id/events/:deliveryHandle` endpoints on bridges and effects.
 * `status`, `replay`, `output`, and `proofs` all live on
 * `response.data.meta` alongside the standard record envelope.
 */
export declare class EventDeliveryResponse extends BaseRecordResponse<LedgerEventDelivery, EventDeliveryMeta> {
    /** Delivery record data extracted from `response.data.data`. */
    delivery: LedgerEventDelivery;
    constructor(response: AxiosResponse<LedgerRecord<LedgerEventDelivery, EventDeliveryMeta>>);
}
