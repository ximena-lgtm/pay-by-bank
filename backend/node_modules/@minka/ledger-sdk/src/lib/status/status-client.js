"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.StatusClient = exports.StatusResponse = void 0;
const base_client_1 = require("../common/clients/base-client");
const base_record_response_1 = require("../common/types/base-record-response");
/**
 * Ledger status API response.
 */
class StatusResponse extends base_record_response_1.BaseRecordResponse {
    /**
     * Record data extracted from the `data` property of the response body.
     */
    status;
    constructor(response) {
        super(response);
        this.status = response?.data?.data;
    }
}
exports.StatusResponse = StatusResponse;
/**
 * Allows querying the ledger status.
 *
 * @see {LedgerStatus}
 */
class StatusClient extends base_client_1.BaseClient {
    /**
     * Returns the basic info about a ledger instance. This request
     * can be used to validate ledger URLs and to check if an instance
     * is up and running.
     *
     * @see {LedgerStatus}
     * @returns the ledger status info
     */
    async read() {
        return this.getRecord('', StatusResponse);
    }
    /**
     * This client doesn't have any remote entity to handle,
     * so this method is a no-op.
     * TODO: Write an special client for entities that directly
     * extends base-client.
     */
    init() {
        return null;
    }
}
exports.StatusClient = StatusClient;
//# sourceMappingURL=status-client.js.map