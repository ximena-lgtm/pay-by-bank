/**
 * @module Clients / Anchors
 * @category Entities
 */
import { AxiosResponse } from 'axios';
import { LedgerAnchor, LedgerIdentifier, LedgerPagedList, LedgerRecord } from "../../../types/src";
import { AccessClient } from '../common/clients/access-client';
import { BaseChangeClient } from '../common/clients/base-change-client';
import { BaseClient, BaseClientOptions } from '../common/clients/base-client';
import { CreateRecordBuilder } from '../common/services/create-record-builder';
import { DropRecordBuilder } from '../common/services/drop-record-builder';
import { UpdateRecordBuilder } from '../common/services/update-record-builder';
import { BaseRecordResponse } from '../common/types/base-record-response';
import { BaseListResponse } from '../common/types/list-response';
import { BaseReadOptions } from '../common/types/read-options';
import { AnchorListParams } from './anchor-list-params';
/**
 * Single anchor API response.
 */
export declare class AnchorResponse extends BaseRecordResponse<LedgerAnchor> {
    /**
     * Record data extracted from the `data` property of the response body.
     */
    anchor: LedgerAnchor;
    constructor(response: AxiosResponse<LedgerRecord<LedgerAnchor>, LedgerRecord<LedgerAnchor>>);
}
/**
 * Anchors list API response.
 */
export declare class AnchorsResponse extends BaseListResponse<LedgerAnchor> {
    /**
     * A flattened list of records, extracted from the `data` property of
     * each record returned.
     */
    anchors: LedgerAnchor[];
    constructor(response: AxiosResponse<LedgerPagedList<LedgerAnchor>, void>);
}
/**
 * Allows querying and managing anchors.
 * @see {LedgerAnchor}
 */
export declare class AnchorClient extends BaseClient {
    /**
     * Allows querying changes.
     *
     */
    change: BaseChangeClient<LedgerAnchor>;
    /**
     * Allows checking access.
     *
     */
    access: AccessClient;
    constructor(options: BaseClientOptions);
    /**
     * Initializes a builder instance that supports chaining to make it
     * easier to work with ledger records. You can pass the record
     * payload to this method to use as initial content, this is useful
     * when you already have an object ready, for example when validating
     * API responses or modifying an existing record. An empty record is
     * going to be created if no initial data is provided.
     * @see {LedgerAnchor}
     * @see {CreateRecordBuilder}
     * @param record initial record payload (optional)
     * @returns a builder instance
     */
    init(record?: Partial<LedgerRecord<LedgerAnchor>>): CreateRecordBuilder<LedgerAnchor, AnchorResponse, import("../../../types/src").LedgerMeta>;
    /**
     * Initializes a builder instance for updating existing records.
     * The builder supports chaining to make it easier to work with
     * ledger records. You can pass the existing record payload to
     * this method to use as initial content.
     * @see {LedgerAnchor}
     * @see {UpdateRecordBuilder}
     * @param record initial record payload
     * @returns a builder instance
     */
    from(record: LedgerRecord<LedgerAnchor>): UpdateRecordBuilder<LedgerAnchor, AnchorResponse, import("../../../types/src").LedgerMeta>;
    /**
     * Initializes a drop builder instance for a Anchor record.
     *  Overloads:
     * - `drop(id?: LedgerIdentifier): DropRecordBuilder<LedgerAnchor>`
     *   Initializes a builder for the Anchor with the given identifier (luid or handle).
     *   If no id is provided, uses the default client id.
     *
     * - `drop(record: LedgerRecord<LedgerAnchor>): DropRecordBuilder<LedgerAnchor>`
     *   Initializes a builder using the provided Anchor record as the initial content.
     *   Useful for dropping an existing record without the additional read call.
     * @see {LedgerAnchor}
     * @see {DropRecordBuilder}
     * @param input Anchor identifier (luid or handle), or a Anchor record.
     * @returns a builder instance
     */
    drop(id?: LedgerIdentifier): DropRecordBuilder<LedgerAnchor>;
    drop(record?: LedgerRecord<LedgerAnchor>): DropRecordBuilder<LedgerAnchor>;
    /**
     * Gets a ledger anchor by identifier (luid or handle).
     * @see {LedgerAnchor}
     * @param id a unique anchor handle
     * @param options read options (optional)
     * @throws {Error} if the anchor doesn't exist or the
     *   current user doesn't have access to this anchor
     * @returns a single anchor response with the requested anchor
     */
    read(id: LedgerIdentifier, options?: BaseReadOptions): Promise<AnchorResponse>;
    /**
     * Queries anchors from a ledger.
     * @see {LedgerAnchor}
     * @param params query parameters (optional)
     * @returns anchors list response with all found anchors
     */
    list(params?: AnchorListParams): Promise<AnchorsResponse>;
}
