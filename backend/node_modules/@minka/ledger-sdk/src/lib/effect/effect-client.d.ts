/**
 * @module Clients / Effects
 * @category Entities
 */
import { AxiosResponse } from 'axios';
import { EffectActivationOptions, LedgerEffect, LedgerIdentifier, LedgerPagedList, LedgerRecord } from "../../../types/src";
import { AccessClient } from '../common/clients/access-client';
import { BaseChangeClient } from '../common/clients/base-change-client';
import { BaseClient, BaseClientOptions } from '../common/clients/base-client';
import { CreateRecordBuilder } from '../common/services/create-record-builder';
import { DropRecordBuilder } from '../common/services/drop-record-builder';
import { UpdateRecordBuilder } from '../common/services/update-record-builder';
import { BaseRecordResponse } from '../common/types/base-record-response';
import { BaseResponse } from '../common/types/base-response';
import { BaseListResponse } from '../common/types/list-response';
import { BaseReadOptions } from '../common/types/read-options';
import { ActivateEffectBuilder } from './activate-effect-builder';
import { EffectEventsClient } from './effect-events-client';
import { EffectListParams } from './effect-list-params';
/**
 * Single effect API response.
 */
export declare class EffectResponse extends BaseRecordResponse<LedgerEffect> {
    /**
     * Record data extracted from the `data` property of the response body.
     */
    effect: LedgerEffect;
    constructor(response: AxiosResponse<LedgerRecord<LedgerEffect>, LedgerRecord<LedgerEffect>>);
}
/**
 * Effect activation API response.
 */
export declare class EffectActivationResponse extends BaseResponse {
}
/**
 * Effects list API response.
 */
export declare class EffectsResponse extends BaseListResponse<LedgerEffect> {
    effects: LedgerEffect[];
    constructor(response: AxiosResponse<LedgerPagedList<LedgerEffect>, void>);
}
/**
 * Allows querying and managing effects.
 * @see {LedgerEffect}
 */
export declare class EffectClient extends BaseClient {
    /**
     * Allows querying changes.
     *
     */
    change: BaseChangeClient<LedgerEffect>;
    /**
     * Allows checking access.
     *
     */
    access: AccessClient;
    /**
     * Allows managing events.
     *
     */
    events: EffectEventsClient;
    constructor(options: BaseClientOptions);
    /**
     * Initializes a builder instance that supports chaining to make it
     * easier to work with ledger records. You can pass the record
     * payload to this method to use as initial content, this is useful
     * when you already have an object ready, for example when validating
     * API responses or modifying an existing record. An empty record is
     * going to be created if no initial data is provided.
     * @see {LedgerEffect}
     * @see {CreateRecordBuilder}
     * @param record initial record payload (optional)
     * @returns a builder instance
     */
    init(record?: Partial<LedgerRecord<LedgerEffect>>): CreateRecordBuilder<LedgerEffect, EffectResponse, import("../../../types/src").LedgerMeta>;
    /**
     * Initializes a builder instance for activating existing record.
     * The builder supports chaining to make it easier to work with
     * ledger records. You can pass the new record payload to this
     * method to use as initial content.
     * @param payload initial activation payload
     * @returns a builder instance
     */
    activate(payload: EffectActivationOptions): ActivateEffectBuilder;
    /**
     * Initializes a builder instance for updating existing records.
     * The builder supports chaining to make it easier to work with
     * ledger records. You can pass the existing record payload to
     * this method to use as initial content.
     * @see {LedgerEffect}
     * @see {UpdateRecordBuilder}
     * @param record initial record payload
     * @returns a builder instance
     */
    from(record: LedgerRecord<LedgerEffect>): UpdateRecordBuilder<LedgerEffect, EffectResponse, import("../../../types/src").LedgerMeta>;
    /**
     * Gets a ledger effect by identifier (luid or handle).
     * @see {LedgerEffect}
     * @param id a unique effect handle
     * @param options read options (optional)
     * @throws {Error} if the effect doesn't exist or the
     *   current user doesn't have access to this effect
     * @returns a single effect response with the requested effect
     */
    read(id: LedgerIdentifier, options?: BaseReadOptions): Promise<EffectResponse>;
    /**
     * Queries effects from a ledger.
     * @see {LedgerEffect}
     * @param params query parameters
     * @returns effects list response with all found effects
     */
    list(params?: EffectListParams): Promise<EffectsResponse>;
    /**
     * Initializes a drop builder instance for a Effect record.
     *  Overloads:
     * - `drop(id?: LedgerIdentifier): DropRecordBuilder<LedgerEffect>`
     *   Initializes a builder for the Effect with the given identifier (luid or handle).
     *   If no id is provided, uses the default client id.
     *
     * - `drop(record: LedgerRecord<LedgerEffect>): DropRecordBuilder<LedgerEffect>`
     *   Initializes a builder using the provided Effect record as the initial content.
     *   Useful for dropping an existing record without the additional read call.
     * @see {LedgerEffect}
     * @see {DropRecordBuilder}
     * @param input Effect identifier (luid or handle), or a Effect record.
     * @returns a builder instance
     */
    drop(id?: LedgerIdentifier): DropRecordBuilder<LedgerEffect>;
    drop(record?: LedgerRecord<LedgerEffect>): DropRecordBuilder<LedgerEffect>;
}
