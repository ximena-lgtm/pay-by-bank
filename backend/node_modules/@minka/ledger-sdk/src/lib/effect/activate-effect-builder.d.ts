/**
 * @module RecordBuilder / ActivateEffectBuilder
 * @category Record Builders
 */
import { EffectActivationOptions, LedgerIdentifier, LedgerRecord } from "../../../types/src";
import { BaseCompositeBuilder } from '../common/services/base-composite-builder';
import { RequestOptions } from '../common/types/request-options';
import { EffectActivationResponse } from './effect-client';
type SendActivationFunction = (record: LedgerRecord<EffectActivationOptions>, effect: LedgerIdentifier, reqOptions?: RequestOptions) => Promise<EffectActivationResponse>;
/**
 * Specific builder for activating an effect.
 *
 * @example
 * // Activating an existing effect
 * const { effect } = await sdk.effect
 *  .with(<effect handle>)
 *  .activate({
 *    maxAge: <maxAge>
 *  })
 *  .hash()
 *  .sign([{ keyPair }])
 *  .send()
 */
export declare class ActivateEffectBuilder extends BaseCompositeBuilder<EffectActivationOptions, EffectActivationResponse> {
    /**
     * A handler function used to submit a signature to ledger.
     */
    private readonly sendActivation;
    /**
     * Identifier of effect on remote server i.e. handle or luid
     */
    private readonly effect;
    constructor(sendActivation: SendActivationFunction, effect: LedgerIdentifier, payload: EffectActivationOptions);
    /**
     * Sends activation request to ledger.
     *
     * @param requestOptions Optional request options to use for this request
     * @returns api response
     * @throws if any method in builder chain fails
     */
    send(requestOptions?: RequestOptions): Promise<EffectActivationResponse>;
}
export {};
