/**
 * @module Clients / Intents
 * @category Entities
 */
import { AxiosResponse } from 'axios';
import { LedgerRecord, TransferTimeseriesData, TransferTimeseriesTotals, TransferTimeseriesWindowItem } from "../../../types/src";
import { BaseRecordResponse } from '../common/types/base-record-response';
/**
 * Intent transfers named query API response.
 * Provides pre-aggregated timeseries data for transfer intents.
 */
export declare class IntentTransferTimeseriesResponse extends BaseRecordResponse<TransferTimeseriesData> {
    /**
     * Per-period timeseries items with status counts, symbol amounts, and latency.
     */
    items: TransferTimeseriesWindowItem[];
    /**
     * Aggregate totals across all time windows.
     */
    totals: TransferTimeseriesTotals;
    constructor(response: AxiosResponse<LedgerRecord<TransferTimeseriesData>, LedgerRecord<TransferTimeseriesData>>);
}
