/**
 * @module RecordBuilder / NamedQueryDataRecordBuilder
 * @category Record Builders
 */
import { LedgerMeta, LedgerRecord } from "../../../../types/src";
import { RequestOptions } from '../types/request-options';
import { BaseRecordBuilder } from './base-record-builder';
/**
 * A handler for named query endpoints that return a single data record
 * (not a paged list) where the request body type and the response type differ.
 */
export type NamedQueryDataRecordHandler<TReq, R, M extends LedgerMeta = LedgerMeta> = (record: LedgerRecord<TReq, M>, reqOptions?: RequestOptions) => Promise<R>;
/**
 * Implements a builder pattern for named queries where the response is a
 * single record (not a list). Supports the standard chain:
 * `.data()`, `.hash()`, `.sign()`, `.send()`.
 */
export declare class NamedQueryDataRecordBuilder<TReq, R, M extends LedgerMeta = LedgerMeta> extends BaseRecordBuilder<TReq, M> {
    protected performNamedQuery: NamedQueryDataRecordHandler<TReq, R, M>;
    constructor(performNamedQuery: NamedQueryDataRecordHandler<TReq, R, M>);
    send(requestOptions?: RequestOptions): Promise<R>;
}
