/**
 * @module RecordBuilder / NamedQueryRecordBuilder
 * @category Record Builders
 */
import { LedgerMeta, LedgerRecord } from "../../../../types/src";
import { BaseListResponse } from '../types/list-response';
import { RequestOptions } from '../types/request-options';
import { BaseRecordBuilder } from './base-record-builder';
/**
 * A handler for named query endpoints on the remote API where the request
 * body type and the response item type differ.
 */
export type NamedQueryRecordHandler<TReq, R, M extends LedgerMeta = LedgerMeta> = (record: LedgerRecord<TReq, M>, reqOptions?: RequestOptions) => Promise<R>;
/**
 * Implements a builder pattern for named queries where the request body
 * type and the response item type are different. Supports the standard
 * chain: `.data()`, `.hash()`, `.sign()`, `.send()`.
 */
export declare class NamedQueryRecordBuilder<TReq, TRes, R extends BaseListResponse<TRes, M>, M extends LedgerMeta = LedgerMeta> extends BaseRecordBuilder<TReq, M> {
    protected performNamedQuery: NamedQueryRecordHandler<TReq, R, M>;
    constructor(performNamedQuery: NamedQueryRecordHandler<TReq, R, M>);
    send(requestOptions?: RequestOptions): Promise<R>;
}
