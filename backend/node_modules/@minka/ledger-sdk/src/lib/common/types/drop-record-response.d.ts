import { AxiosResponse } from 'axios';
/**
 * Base API response for record deletions. The full response from
 * the http client - axios - is available to
 * support more advanced use cases.
 */
export declare class DropRecordResponse {
    /**
     * The raw server response.
     */
    response: AxiosResponse;
    constructor(response: AxiosResponse);
}
/**
 * A type that covers all RecordResponse classes. This type can be used
 * to pass response classes to functions so that those functions can
 * construct them when a response is received.
 */
export type DropRecordResponseClass = new (response: AxiosResponse) => DropRecordResponse;
