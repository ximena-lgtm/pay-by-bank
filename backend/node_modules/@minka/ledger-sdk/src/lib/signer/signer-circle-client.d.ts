/**
 * @module Clients / Signer Circles
 * @category Entities
 */
import { CircleSignerListParams } from '../circle/circle-list-params';
import { CircleSignersResponse } from '../circle/circle-signer-client';
import { BaseClient, BaseClientOptions } from '../common/clients/base-client';
/**
 * Allows querying circles.
 *
 * @see {LedgerCircleSigner}
 */
export declare class SignerCircleClient extends BaseClient {
    private parentPath;
    constructor(options: BaseClientOptions, parentPath: string);
    private buildBasePath;
    /**
     * Queries circles of a signer from a ledger.
     *
     * @see {LedgerCircleSigner}
     * @param params query parameters
     * @returns circle signers list response with all found circles
     */
    list(params?: CircleSignerListParams): Promise<CircleSignersResponse>;
}
