/**
 * @module Clients / Ledger Access
 * @category Entities
 */
import { AccessClient } from '../common/clients/access-client';
/**
 * Allows checking ledger access.
 *
 * @see {Ledger}
 */
export declare class LedgerAccessClient extends AccessClient {
    protected buildBasePath(): string;
}
