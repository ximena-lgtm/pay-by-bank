import { JwtConfig } from "../../../types/src";
import { HeaderValue } from '../common/types/headers';
export type TimeseriesListParams = {
    query?: {
        starts: Date;
        closes: Date;
        period: '1h' | '1d';
        reduce: 'mean' | 'sum' | 'min' | 'max' | 'count' | 'p50' | 'p95' | 'p99' | 'latest' | 'delta';
        groups?: string | string[];
        sample?: string;
        filter?: Record<string, unknown>;
    };
    authParams?: Partial<JwtConfig>;
    headers?: Record<string, HeaderValue>;
};
