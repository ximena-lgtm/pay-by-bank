/**
 * @module Clients / Metrics
 * @category Entities
 */
import { AxiosResponse } from 'axios';
import { LedgerIdentifier, LedgerMetric, LedgerPagedList, LedgerRecord, MetricTimeseriesItem } from "../../../types/src";
import { BaseClient, BaseClientOptions } from '../common/clients/base-client';
import { UpdateRecordBuilder } from '../common/services/update-record-builder';
import { BaseRecordResponse } from '../common/types/base-record-response';
import { BaseListResponse } from '../common/types/list-response';
import { BaseReadOptions } from '../common/types/read-options';
import { TimeseriesListParams } from './timeseries-list-params';
/**
 * Single metric API response.
 */
export declare class MetricResponse extends BaseRecordResponse<LedgerMetric> {
    /**
     * Record data extracted from the `data` property of the response body.
     */
    metric: LedgerMetric;
    constructor(response: AxiosResponse<LedgerRecord<LedgerMetric>, LedgerRecord<LedgerMetric>>);
}
/**
 * Metrics list API response.
 */
export declare class MetricsResponse extends BaseListResponse<LedgerMetric> {
    /**
     * A flattened list of records, extracted from the `data` property of
     * each record returned.
     */
    metrics: LedgerMetric[];
    constructor(response: AxiosResponse<LedgerPagedList<LedgerMetric>, void>);
}
/**
 * Metrics list API response.
 */
export declare class MetricTimeseriesListResponse extends BaseRecordResponse<MetricTimeseriesItem[]> {
    /**
     * A flattened list of records, extracted from the `data` property of
     * each record returned.
     */
    timeseries: MetricTimeseriesItem[];
    constructor(response: AxiosResponse<LedgerRecord<MetricTimeseriesItem[]>>);
}
/**
 * Allows querying and managing metrics.
 * @see {LedgerMetric}
 */
export declare class MetricClient extends BaseClient {
    constructor(options: BaseClientOptions);
    /**
     * Gets a ledger metric by identifier (luid or handle).
     * @see {LedgerMetric}
     * @param options
     * @throws {Error} if the metric doesn't exist or the
     *   current user doesn't have access to this metric
     * @param id a unique metric handle
     * @returns a single metric response with the requested metric
     */
    read(id: LedgerIdentifier, options?: BaseReadOptions): Promise<MetricResponse>;
    /**
     * Initializes a builder instance for updating existing records.
     * The builder supports chaining to make it easier to work with
     * ledger records. You can pass the existing record payload to
     * this method to use as initial content.
     * @see {LedgerMetric}
     * @see {UpdateRecordBuilder}
     * @param record initial record payload
     * @returns a builder instance
     */
    from(record: LedgerRecord<LedgerMetric>): UpdateRecordBuilder<LedgerMetric, MetricResponse, import("../../../types/src").LedgerMeta>;
    /**
     * Queries metrics from a ledger.
     * @see {LedgerMetric}
     * @param id
     * @param options
     * @param params query parameters
     * @returns metrics list response with all found metrics
     */
    listTimeseries(id: LedgerIdentifier, options?: TimeseriesListParams): Promise<MetricTimeseriesListResponse>;
}
