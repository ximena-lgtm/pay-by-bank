"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.generateIntentHandle = exports.ROUTING_CODES = void 0;
/**
 * Routing codes used in intent handle generation
 */
exports.ROUTING_CODES = ['TFY', 'ENT', 'CRB', 'VIS', 'SRV'];
/**
 * Generates an intent handle based on timestamp, NIT, routing code, and sequence
 * @param timestamp - Date to extract year, month, and day from
 * @param nit - National Tax Identification number
 * @param routingCode - Routing code identifier
 * @param sequence - Sequential identifier
 * @returns Formatted intent handle string
 * @example
 * ```typescript
 * const timestamp = new Date('2023-12-25')
 * const handle = generateIntentHandle(timestamp, '123456789', 'TFY', '001')
 * // Returns: '20231225123456789TFY001'
 * ```
 */
const generateIntentHandle = (timestamp, nit, routingCode, sequence) => {
    return (timestamp.getFullYear().toString() +
        (timestamp.getMonth() + 1).toString().padStart(2, '0') +
        timestamp.getDate().toString().padStart(2, '0') +
        nit +
        routingCode +
        sequence);
};
exports.generateIntentHandle = generateIntentHandle;
//# sourceMappingURL=intent-handle.js.map