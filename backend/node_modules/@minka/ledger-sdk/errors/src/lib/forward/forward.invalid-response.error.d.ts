import { LedgerCustom, LedgerError, LedgerErrorReason } from '../common';
/**
 * Error thrown when ledger is not able to parse the response of a forwarded
 * request. Possible reasons:
 *  - proxy system didn't sign the response correctly (e.g. signature is missing or invalid)
 *  - proxy system responded without a response body (e.g. response is empty)
 */
export declare class ForwardInvalidResponse extends LedgerError {
    /**
     *
     * @param detail error detail - message
     * @param custom custom data record to attach
     *               additional info about the error
     */
    constructor(detail: string, custom?: LedgerCustom);
    static REASON: LedgerErrorReason;
}
