import { SortDirection } from './sort-direction';
/**
 * Sort options for entities. Key ordering is guaranteed in order of insertion, so sorting by multiple fields is possible and consistent.
 */
type SortOptions<EntitySortableFields extends string = string> = Map<EntitySortableFields, SortDirection>;
export { SortOptions };
