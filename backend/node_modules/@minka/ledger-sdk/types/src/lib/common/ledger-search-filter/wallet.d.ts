import { LedgerHandle } from '../ledger-handle';
import { DefaultBaseSearchableFields, SearchableFields } from './base';
type BaseWalletSearchableFields = {
    /**
     * Domain handle(s), or boolean in case of usage of `$isNull` operator.
     */
    'meta.domain': string;
    'data.schema': LedgerHandle;
};
type WalletSearchableFields = Partial<SearchableFields<BaseWalletSearchableFields & DefaultBaseSearchableFields>>;
export { WalletSearchableFields };
