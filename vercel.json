{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "comment": "Todo lo que no sea /api/* lo resuelve el router del cliente",
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
